import { Icon, LucideProps } from "lucide-react";
import { steeringWheel } from "@lucide/lab";

export const SteeringWheel = (props: LucideProps) => {
  return <Icon iconNode={steeringWheel} {...props} />;
};
